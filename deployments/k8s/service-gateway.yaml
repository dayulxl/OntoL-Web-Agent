apiVersion: v1
kind: Service
metadata:
  name: langgraph-gateway
  labels:
    app: langgraph-gateway
spec:
  type: ClusterIP
  selector:
    app: langgraph-gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
